# Shikimori dating app

## Getting Started

First, init the project (it will build docker images, install deps, etc):

```bash
make init
```

Than, run the development server:

```bash
make server
```
